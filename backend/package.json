{
  "name": "mvc-express",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon index.js",
    "migrate": "node migrate.js",
    "start": "node index.js",
    "lint": "echo '----- Checking Backend...' && npx prettier --debug-check . && eslint --ext .js . && echo '----- Done!'",
    "fix": "prettier --write . && eslint --fix --ext .js .",
    "test": "set DATABASE_URL=postgresql://glemoine:colombia@localhost:5432/aidna_test?schema=public && jest",
    "test-bail" : "set DATABASE_URL=postgresql://glemoine:colombia@localhost:5432/aidna_test?schema=public && jest --bail",
    "test:watch": "set DATABASE_URL=postgresql://glemoine:colombia@localhost:5432/aidna_test?schema=public && jest --watchAll"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^4.9.0",
    "argon2": "^0.30.3",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.2",
    "joi": "^17.9.0",
    "jsonwebtoken": "^9.0.0",
    "multer": "^1.4.5-lts.1",
    "mysql2": "^2.3.3",
    "nodemailer": "^6.9.1",
    "template-fullstack": "file:.."
  },
  "rules": {
    "no-console": [
      "error",
      {
        "allow": [
          "warn",
          "error"
        ]
      }
    ]
  },
  "devDependencies": {
    "eslint": "^8.11.0",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-config-prettier": "^8.5.0",
    "eslint-plugin-import": "^2.25.4",
    "eslint-plugin-prettier": "^4.0.0",
    "jest": "^29.6.1",
    "nodemon": "^2.0.15",
    "prettier": "2.5.1",
    "prisma": "^4.9.0",
    "supertest": "^6.3.3"
  }
}
